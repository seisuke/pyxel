{
  "tasks": {
    "build": "zsh ./build.sh",
    "local": "deno run --allow-read src/main.ts",
    "local_all": "deno task build && deno task run"
    "serve": "deno run --allow-read --allow-net src/server.ts",
    "web_all": "deno task build && deno task serve"
  }
}